Please do a no-full-rerun cleanup pass for the LongMemEval-S semantic-memory blog post. Do not launch a new full 500-question run. Use existing run stores, manifests, caches, and analysis scripts only.

Need exact values / checks:

1. Matched-hit subset test
   - For deterministic vs flat-LLM, restrict to answerable questions where both systems have turn_hit=1.
   - Report n, accuracy for each system on that subset, paired McNemar b/c/net/p.
   - This tests whether the lower acc|hit=1 under LLM extraction is a selection effect.

2. Abstention accuracy
   - There are 30 abstention questions.
   - Report abstention accuracy by run:
     full-s-sonnet, task18-flat-500, task18-agentic-500, task19-retain-500.
   - Also report false-positive answer count if available.
   - Add a small table for the blog.

3. Hit=1 error fidelity audit
   - For flat-LLM and retain, audit evidence-present wrong answers.
   - Count how many are likely:
     a) genuine reasoning/evidence-use failures,
     b) compression/fidelity/span-loss failures,
     c) context ordering/noise/conflict failures,
     d) unclear.
   - The goal is to avoid overstating “reasoning failure” when turn_hit only proves provenance coverage.

4. Scaffold alias check
   - Confirm whether the parity 50-question slice and scaffold 50-question slice actually resolved to the same Claude Sonnet snapshot.
   - If manifest alias cannot be resolved, say so explicitly.
   - No full run needed; just inspect logs/manifests/provider records.
   - If there is a cheap way to rerun only the parity 50 slice with pinned -20250929, note cost/time, but do not run unless already cached and trivial.

5. Flat-correct / retain-wrong assistant question
   - Confirm that the single flat-only correct single-session-assistant item is 58470ed2, the Borges quote.
   - Produce a one-row audit: flat evidence snippet, retain evidence snippet, whether source memory existed, whether exact quote terms were present, and judge decisions.
   - The blog now frames this as “one new retain regression,” not “one non-recovered original failure.”

6. Write-time cost table if available
   - For task18-flat-500 and task19-retain-500, extract:
     extractor calls,
     extractor input tokens,
     extractor output tokens,
     embedding calls/tokens,
     number of memories before/after consolidation if available,
     storage size if available,
     reader context tokens,
     reader output tokens.
   - If some fields are unavailable, report “not recorded” rather than estimating.

7. Reproduction metadata
   - Full dataset sha256, not truncated.
   - Exact extractor model snapshot or alias resolution for gpt-4o-mini.
   - Judge prompt path or prompt hash.
   - Exact 50-question scaffold slice IDs or a stable hash of them.
   - Confirm Appendix A CLI supports --retain rather than passing retain through legacy --agentic. If not, either update script flags or document the legacy naming.

Return:
- concise tables only;
- no prose rewrite;
- no full rerun;
- no new claims unsupported by existing artifacts.